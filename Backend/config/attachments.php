<?php

use App\Models\Payment;

return [

    /*
    |--------------------------------------------------------------------------
    | Disk افتراضي للمرفقات الحساسة
    |--------------------------------------------------------------------------
    | جميع المرفقات (إثباتات دفع، هويات، رخص، شهادات) تُخزَّن هنا افتراضيًا —
    | disk خاص غير قابل للوصول المباشر عبر رابط عام (راجع config/filesystems.php).
    */
    'disk' => env('ATTACHMENTS_DISK', 'attachments'),

    /*
    |--------------------------------------------------------------------------
    | الحد الأقصى لحجم الملف (كيلوبايت)
    |--------------------------------------------------------------------------
    */
    'max_size_kb' => (int) env('ATTACHMENTS_MAX_SIZE_KB', 5120), // 5MB

    /*
    |--------------------------------------------------------------------------
    | أنواع MIME المسموحة
    |--------------------------------------------------------------------------
    | يُقصَد بها الأنواع المسموحة افتراضيًا لكل المرفقات بالنظام. أي Document
    | Type يحتاج قيودًا أخص (مثلاً: هوية = صورة فقط) يُطبَّق ضمن الـ FormRequest
    | الخاص بالدومين (StorePaymentRequest مثلاً) وليس هنا.
    */
    'allowed_mimes' => [
        'image/jpeg',
        'image/png',
        'image/webp',
        'application/pdf',
    ],

    /*
    |--------------------------------------------------------------------------
    | مدة الاحتفاظ بالملف الفعلي بعد الحذف الناعم (بالأيام)
    |--------------------------------------------------------------------------
    | بعدها يقوم PruneAttachmentsCommand بحذف الملف من القرص نهائيًا + forceDelete
    | للسجل. راجع routes/console.php للجدولة، و app/Console/Commands/PruneAttachmentsCommand.php
    */
    'retention_days' => (int) env('ATTACHMENTS_RETENTION_DAYS', 30),

    /*
    |--------------------------------------------------------------------------
    | الكيانات المسموح إرفاق ملفات بها (Whitelist)
    |--------------------------------------------------------------------------
    | تربط اسمًا حرًّا (يُستخدم لاحقًا كقيمة attachable_type قادمة من الـ Client
    | عند وجود Endpoint عام للرفع) بكلاس الـ Model الفعلي — يمنع تمرير أي
    | Class Name عشوائي من الخارج (Type Confusion / IDOR). راجع التعليق أدناه:
    | لا يوجد حاليًا Endpoint عام يستهلك هذه المصفوفة (StoreAttachmentRequest
    | محذوف عمدًا — راجع القرار المعماري)، وهي موجودة هنا فقط لتوثيق نطاق
    | البنية العامة القابلة للتوسع دون الحاجة لإعادة تصميم config لاحقًا.
    | فعّلي كل سطر فقط عند ربط الموديل المقابل فعليًا بـ HasAttachments +
    | Service مختص برفع مرفقاته.
    */
    'allowed_attachable_types' => [
        'payment' => Payment::class,
        // 'generator' => \App\Models\Generator::class,
        // 'technician' => \App\Models\Technician::class,
        // 'subscription' => \App\Models\Subscription::class,
        // 'user' => \App\Models\User::class,
    ],

];
